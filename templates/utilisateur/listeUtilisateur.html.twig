{% extends 'base.html.twig' %}{% block title %}{{ parent()}} Liste des utilisateurs {% endblock %}{% block contenu %}    <h1> Liste des utilisateurs </h1>    {% if not listeUtilisateurs is empty %}    <table class="table">    <tr><th>Nom</th><th>Prénom</th><th>Date de naissance</th></tr>     {% for key,value in listeUtilisateurs %}        <tr><td><a href="">{{ value.nom }}</a></td><td>{{ value.prenom }}</td><td>{{ value.datenaissance|date("d/m/Y") }}</td></tr>     {% endfor %}    </table>{% else %}    Aucun utilisateur à afficher{% endif %}{% endblock %}